import Draggable from '@/core/components/Draggable.vue'
import { shallowMount } from '@vue/test-utils'

describe('Draggable.vue', () => {
  // const propsData = {
  //   items: [{ title: 'Task 1', description: 'Task 1 description' }],
  //   status: 'Opened',
  //   text: 'Open Tasks',
  //   disabled: false,
  // }
  //
  // it('renders with correct props', () => {
  //   const wrapper = shallowMount(Draggable, {
  //     propsData,
  //   })
  //
  //   expect(wrapper.props('items')).toEqual(propsData.items)
  //   expect(wrapper.props('status')).toEqual(propsData.status)
  //   expect(wrapper.props('text')).toEqual(propsData.text)
  //   expect(wrapper.props('disabled')).toEqual(propsData.disabled)
  // })
})
